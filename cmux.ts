/**
 * pi-cmux extension entrypoint.
 *
 * Re-exports from modular src/ — this file exists because
 * pi extensions reference a single .ts file in package.json.
 */
export { default } from "./src/index.js";
